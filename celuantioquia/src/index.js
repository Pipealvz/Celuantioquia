import React from 'react';
import ReactDOM from 'react-dom/client';
import RoutesIndex from './Routes/RoutesLogin';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode >

    <RoutesIndex />

  </React.StrictMode>
);